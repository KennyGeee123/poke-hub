FROZEN — launch-ready working state 2026-09-22 (adventure basemap)
Live: https://pokedex-hub-lime.vercel.app/
Lock: launch-lock/WORKING-STATE.json
Tag:  launch-ready-2026-09-22-adventure-basemap

Change: AdventureWorldMap defaults to Esri World Dark Gray Base / World
Street Map tiles (z/y/x) so Carto "API KEY REQUIRED" watermarks are gone
without secrets. Optional VITE_CARTO_API_KEY or VITE_MAP_TILE_KEY prefers
Carto dark. Attribution footer: Basemap © Esri.

Do not apply drive-by / "hack" edits to hashed files.
To thaw for a real change: update the files, rebuild WORKING-STATE.json from a
verified live launch, then git tag.
